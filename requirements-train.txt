# Extra dependencies needed ONLY for downloading datasets and training models
# (not required to run the app or the demo). Install with:
#   pip install -r requirements-train.txt
roboflow>=1.1.0
kaggle>=1.6.0
